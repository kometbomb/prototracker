version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: Install deps
          command: sudo apt-get install build-essential sdl2 libsdl2-image-dev
      - checkout
      - run:
          name: Build
          command: make linux

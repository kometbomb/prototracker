version: 2
jobs:
  build:
    docker:
      - image: gcc:5
    steps:
      - run:
          name: Install deps
          command: apt-get install sdl2 libsdl2-image-dev
      - checkout
      - run:
          name: Build
          command: make linux
